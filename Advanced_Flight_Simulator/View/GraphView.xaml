<UserControl x:Class="Advanced_Flight_Simulator.GraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Advanced_Flight_Simulator"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d" Height="491.666" Width="569.333">
    <Grid Height="437" Margin="28,68,-33,-13.667" VerticalAlignment="Top" HorizontalAlignment="Left" Width="665">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="159*"/>
            <ColumnDefinition Width="10*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="17*"/>
            <RowDefinition Height="90*"/>
        </Grid.RowDefinitions>
        <ListBox Name="options"  Background="Transparent" SelectedIndex="0" SelectedValue="{Binding VM_Graph_Attribute}" ItemsSource="{Binding VM_AttributesNames}" Margin="0,0,95,109.333" Width="226" Height="247" VerticalAlignment="Bottom" HorizontalAlignment="Right" Grid.Row="1" />

        <oxy:Plot x:Name="plot" Background="Transparent"  Title="{Binding VM_Graph_Attribute}" Margin="9,34,410,0" Height="200" VerticalAlignment="Top" Grid.RowSpan="2">
            <oxy:Plot.Series>
                <oxy:LineSeries Background="Transparent" ItemsSource="{Binding VM_GraphPoints}" DataFieldX="X" DataFieldY="Y"/>
            </oxy:Plot.Series>
        </oxy:Plot>
        <oxy:Plot x:Name="CorrelatedPlot" Background="Transparent"  Title="{Binding VM_Correlated_Attribute}" Margin="-23,0,403,-1.667" Height="198" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="246" Grid.Row="1">
            <oxy:Plot.Series>
                <oxy:LineSeries Background="Transparent" ItemsSource="{Binding VM_Correlated_GraphPoints}" DataFieldX="X" DataFieldY="Y"/>
            </oxy:Plot.Series>
        </oxy:Plot>
    </Grid>
</UserControl>
